on:
  push:
  schedule:
    - cron: '0 0 * * *'
steps:
  - name: Clone repository
    uses: actions/checkout@v2
  - name: Validate Makefile
    run: |
      cd .github/workflows/module3_task0.yml
      make help
